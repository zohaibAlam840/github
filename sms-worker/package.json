{
  "name": "i2i-sms-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Office-side worker for the i2i CMS. Owns the SIM7600 modem on a COM port and exposes it to the local dashboard over HTTP; sends and receives the SMS that drive the TRB141 field gateways. Runs entirely offline.",
  "type": "module",
  "scripts": {
    "worker": "tsx --env-file-if-exists=.env src/index.ts",
    "test-command": "tsx --env-file-if-exists=.env src/testCommand.ts",
    "diagnose": "tsx src/diagnose.ts",
    "selftest": "tsx src/selftest.ts"
  },
  "dependencies": {
    "serialport": "^13.0.0",
    "@serialport/parser-readline": "^13.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^24.0.0"
  }
}
