{
  "name": "i2i-demo-recorder",
  "private": true,
  "type": "module",
  "description": "Records the manual's video tutorials by driving the real dashboard in a browser. A development tool: never shipped to the client's PC.",
  "scripts": {
    "record": "tsx record.ts",
    "seed": "tsx seed.ts"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "playwright": "^1.63.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
